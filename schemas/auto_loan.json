{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://auto_agent/schemas/auto_loan.json",
  "title": "Auto Loan Production fetcher output",
  "description": "Skeleton — field list finalized in data_acquisition.md. additionalProperties stays true until then.",
  "type": "object",
  "required": ["source_type", "source_metadata", "schema_version", "data"],
  "properties": {
    "source_type": { "const": "auto_loan" },
    "schema_version": { "const": "1.0" },
    "source_metadata": {
      "type": "object",
      "description": "Fetcher-specific fields. TODO(data_acquisition): lock fields — expected: timestamp, t0_date, date_range_start, date_range_end.",
      "additionalProperties": true
    },
    "data": {
      "description": "TODO(data_acquisition): lock shape — expected rows with (date, segment, variable, value).",
      "oneOf": [{ "type": "array" }, { "type": "object" }]
    }
  },
  "additionalProperties": true
}
