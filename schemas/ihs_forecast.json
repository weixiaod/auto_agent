{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://auto_agent/schemas/ihs_forecast.json",
  "title": "IHS econ forecast fetcher output",
  "description": "Skeleton — field list finalized in data_acquisition.md. additionalProperties stays true until then.",
  "type": "object",
  "required": ["source_type", "source_metadata", "schema_version", "data"],
  "properties": {
    "source_type": { "const": "ihs_forecast" },
    "schema_version": { "const": "1.0" },
    "source_metadata": {
      "type": "object",
      "description": "Fetcher-specific fields. TODO(data_acquisition): lock fields — expected: publn_id, yymm_num, scenario_var.",
      "additionalProperties": true
    },
    "data": {
      "description": "TODO(data_acquisition): lock shape — expected rows with (date, variable, value) from t0 forward.",
      "oneOf": [{ "type": "array" }, { "type": "object" }]
    }
  },
  "additionalProperties": true
}
